# Copyright (c) HBP 2014-2015

#set(WSSERVER_PUBLIC_HEADERS vocabulary.h)
set(WSSERVER_SOURCES wsserver.cpp)
set(WSSERVER_LINK_LIBRARIES ${LUNCHBOX_LIBRARIES} ${LIBZMQ_LIBRARIES} ${ZEQ_LIBRARIES} wsvoc)
if(MSVC)
  list(APPEND WSSERVER_LINK_LIBRARIES Ws2_32)
endif()

list(APPEND CPPCHECK_EXTRA_ARGS)

set( PROJECT_INCLUDE_NAME WSSERVER )
common_application( wsserver )
